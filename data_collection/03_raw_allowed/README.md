# Raw source storage policy

This pilot intentionally does not mirror full HTML pages or textbook bodies.

The candidate file stores short excerpts, source anchors, hashes, and attribution metadata.
If a future run stores an allowed raw snapshot, it must be placed under a run-specific
subdirectory and must include the source license, retrieval timestamp, canonical URL,
content hash, and a reversible provenance link.
